export enum Order {
  ASC = "ASC",
  DESC = "DESC",
}

export enum StatusEnum {
  ACTIVE = "active",
  INACTIVE = "inactive",
}

export enum StatusProject {
  ACTIVE = "active",
  PENDING = "pending",
  DONE = "done",
}
